{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    // key intentionally left blank to ensure user/passwords are not stored in source control. This value is overwritten
    // by secrets.json locally or Azure App Service configuration. An Azure Key Vault storage would be ideal for this as
    // it avoids setting up a secrets file and separate app config to allow remote managed access, but it's expensive
    "PatientRecordsConnection": ""
  }
}
